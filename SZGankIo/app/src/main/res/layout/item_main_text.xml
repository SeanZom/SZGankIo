<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_parent"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/parent_distance_default">

    <TextView
        android:id="@+id/tv_topic_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/regular_body_text_size"
        android:textColor="@color/black"
        tools:text="Android"/>

    <TextView
        android:id="@+id/tv_who"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_topic_title"
        android:layout_marginTop="8dp"
        android:textSize="@dimen/regular_info_text_size"
        android:textColor="@color/textSecondary"
        tools:text="Sean"/>

    <TextView
        android:id="@+id/tv_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignBaseline="@id/tv_who"
        android:textSize="@dimen/regular_info_text_size"
        android:textColor="@color/textSecondary"
        tools:text="2016-11-22"/>

</RelativeLayout>